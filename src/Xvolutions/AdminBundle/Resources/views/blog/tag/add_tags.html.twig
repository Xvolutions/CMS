{% extends "XvolutionsAdminBundle:template:backoffice.html.twig" %}

{% block title %}{{title|trans}} - Palavras-Chave - Backoffice{% endblock %}

{% block maincontent %}
    <h1>{{ title|trans }}</h1>
    {% if status %}
        <div class="alert alert-success alert-dismissable">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
            {{ status|trans }}
        </div>
    {% endif %}
    {% if error %}
        <div class="alert alert-danger alert-dismissable">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
            {{ error|trans }}
        </div>
    {% endif %}

    {{ form_start( form ) }}
    <div id="xvolutions_adminbundle_tagss"><div>
            <p>{{ "Nome da palavra-chave"|trans }}</p>
            <p>{{ form_widget( form.tag ) }}</p>
        </div>
        <br>
            {{ form_end( form ) }}
            <a href="{{path('tags')}}" class="btn btn-default btn-sm">{{ "Ver Palavras-chave"|trans }}</a>
    </div>
</form>
{% endblock %}