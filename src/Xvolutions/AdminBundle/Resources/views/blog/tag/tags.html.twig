{% extends "XvolutionsAdminBundle:template:backoffice.html.twig" %}

{% block title %}Categorias - Backoffice{% endblock %}

{% block maincontent %}
    <h1>{{ "Palavras-Chave"|trans }}</h1>
        <div id="status" class="alert alert-success alert-dismissable" style="display: none;">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
            <div id="text"></div>
        </div>
        <div id="error" class="alert alert-danger alert-dismissable" style="display: none;">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
            <div id="text-error"></div>
        </div>
    {% if status %}
        <div class="alert alert-success alert-dismissable">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
            {{ status|trans }}
        </div>
    {% endif %}
    {% if error %}
        <div class="alert alert-danger alert-dismissable">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
            {{ error|trans }}
        </div>
    {% endif %}
    <table class="table table-hover">
        <tr>
            <th></th>
            <th>{{ "Palavras-Chave"|trans }}</th>
            <th>{{ "Opções"|trans }}</th>
        </tr>
        {% if tagList|length > 0 %}

            {% for tag in tagList %}
                <tr class="tr{{tag.id}}">
                    <td><input type="checkbox" id="{{tag.id}}" onclick="SelectForRemoval('{{tag.id}}');"></td>
                    <td>{{tag.tag}}</td>
                    <td>
                        <a href="{{path('edittags', {'id': tag.id})}}" type="submit" class="btn btn-primary btn-xs">{{ "Editar"|trans }}</a> 
                        <a class="btn btn-danger btn-xs" name="{{path('optiontags', {'option': 'remove','id': tag.id})}}">{{ "Apagar"|trans }}</a>
                    </td>
                </tr>
            {% endfor %}
        {% else %}
            <tr>
                <td colspan="7" >{{"Não existem Palavras-Change definidas"|trans}}</td>
            <tr>
        {% endif %}
    </table>
    <p>
        <a href="{{path('addcategories')}}" type="submit" class="btn btn-primary btn-sm">{{ "Criar uma nova palavra-chave"|trans }}</a>
        <a class="btn btn-danger btn-sm" name="{{path('optiontags', {'option': 'removeselected'})}}" id="RemoveMany">{{ "Apagar"|trans }}</a>
    </p> 
{% endblock %}