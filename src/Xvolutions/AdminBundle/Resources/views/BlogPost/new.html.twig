{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BlogPost creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('blog_posts') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
